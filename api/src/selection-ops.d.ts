import type { AppContext } from "./app-context.ts";
export declare function selectionToNewLayer({ move }: {
    move: boolean;
}): void;
export declare function initSelectionOps(ctx: AppContext): void;
